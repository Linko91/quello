{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": { "noEmit": true, "types": [], "allowJs": true },
  // `.astro` files are not TypeScript; `astro check` is what validates those.
  "include": ["src/**/*.ts", "astro.config.mjs"]
}
