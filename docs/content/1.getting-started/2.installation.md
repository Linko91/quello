---
title: Installation
description: Pick the route that matches your setup — a Vite plugin, a webpack plugin, or the CLI.
navigation:
  icon: i-lucide-download
---

There are four ways in, and which one you need depends on your **builder**, not your framework.

::card-group
  :::card
  ---
  title: Vite
  icon: i-lucide-zap
  to: /guides/vite
  ---
  Vue, React, Svelte, Solid — and Nuxt, SvelteKit, Astro with one extra line.
  :::

  :::card
  ---
  title: webpack
  icon: i-lucide-box
  to: /guides/webpack
  ---
  webpack 5 with `webpack-dev-server` and `html-webpack-plugin`.
  :::

  :::card
  ---
  title: Next
  icon: i-simple-icons-nextdotjs
  to: /guides/next
  ---
  Its own package, because Next has no plugin API to hook into.
  :::

  :::card
  ---
  title: Anything else
  icon: i-lucide-terminal
  to: /guides/vanilla
  ---
  Angular, Rails, Laravel, plain HTML — run the CLI beside your own server.
  :::
::

## Vite

```bash [Terminal]
pnpm add -D vite-plugin-quello
```

```ts [vite.config.ts]
import { defineConfig } from 'vite'
import quello from 'vite-plugin-quello'

export default defineConfig({
  plugins: [quello()],
})
```

Start your dev server. That is the whole setup for a Vite SPA — the script tag is injected for you.

Frameworks that render their own HTML (Nuxt, SvelteKit, Astro) need [one extra
line](/guides/nuxt), because the hook that injects the tag is never called there.

## webpack

```bash [Terminal]
pnpm add -D webpack-plugin-quello
```

```js [webpack.config.js]
import QuelloWebpackPlugin from 'webpack-plugin-quello'

export default {
  plugins: [
    new HtmlWebpackPlugin({ template: './src/index.html' }),
    new QuelloWebpackPlugin(),
  ],
  devServer: { port: 3000 },
}
```

## Next

```bash [Terminal]
pnpm add -D @quello/next
```

```ts [next.config.ts]
import { withQuello } from '@quello/next/config'

export default withQuello({
  // your config, untouched
})
```

```tsx [app/layout.tsx]
import { Quello } from '@quello/next'

// …inside <body>, once
<Quello />
```

On the next `next dev`, `withQuello` writes the route handler, the agent file and the `.gitignore`
entry, then prints where each went. See the [Next guide](/guides/next).

## No bundler quello can hook into

Angular, a Rails or Laravel app, three HTML files in a folder — anything that will not take a
plugin. Run quello beside it:

```bash [Terminal]
npx quello
```

It prints the tag to paste into your page:

```html [index.html]
<script defer src="http://127.0.0.1:5100/__quello/client.js"
        data-quello-endpoint="http://127.0.0.1:5100/__quello/picks"></script>
```

## Check it worked

Open your app. A small `quello` pill should be sitting in the bottom-right corner. Press `Alt+Q`,
click something, and a `.quello/picks.json` file should appear in your project.

::tip
Add `.quello/` to your `.gitignore`. It describes your current browser session, not your project.
::
